# Solvan technical-debt ledger

Status: active
Owner: Solvan engineering

Debt is recorded here when it is real, bounded, owned, and has a removal
condition. Roadmap ideas belong in product specifications instead.

| ID | Severity | Debt | Why accepted now | Removal condition |
|---|---|---|---|---|
| TD-001 | P1 target | live Gemini safety and statistical-quality runners exist but have not produced passing receipts for the pinned release model | model/project credentials are environment-bound and were not available to the local harness check | pinned live safety and three-repetition quality receipts pass, bind model/project/location, meet every threshold, and record bounded latency/token cost |
| TD-004 | P1 delivery | Git has a local baseline commit but no configured judging remote | publication details are not yet available; rules permit private with judge access, while the documentation criterion rewards public GitHub | clean-clone bootstrap and CI pass from the intended remote; if private, both required judging accounts have verified access |
| TD-009 | P2 correctness | built-in pack publication runs the specification 17 lifecycle directly and creates no `skill_import_attempt`/`skill_import`, though specification 18 §11 calls first-party publication "an ordinary §6 import" | the packs ship inside the release image and are proved by the merge gate, which now executes the loader's own revision conversion, so the import records would attest to a fetch that never happens; inventing them would make the audit trail less true, not more | either implement a real §6 import attempt over the in-repo pack bytes at the pinned commit, or amend §11 and `CT-SKILL-FIRSTPARTY-001` to describe built-in publication as spec-17-native; do not leave the specification and the loader disagreeing |
| TD-013 | P2 correctness | the skills repository connector still authenticates from a static `SOLVAN_GITHUB_INSTALLATION_TOKEN` (`apps/api/agent_skills.py:1621`), so that one path holds a credential that expires after an hour with no refresh | the release provider itself no longer does this: `src/solvan/platform/github_app_auth.py` signs an RS256 App JWT, calls `/app/installations/<id>/access_tokens`, and refreshes before expiry under a per-installation exchange lock. Only the skills metadata read was left on the old shape, and it is inert unless `SOLVAN_SKILLS_REPOSITORY_PROVIDER=github` | route the skills connector through the same `GitHubAppInstallationTokenProvider` and delete the static-token branch |
| TD-014 | P3 maintainability | with the vendor providers withdrawn, no connectable provider produces a `STORED_LONG_LIVED` posture, so the stored-key branch of onboarding, the credential scope-verification service, and the posture's own DDL constraints have no reachable producer | the posture and its verification are correct and fail closed; leaving them costs nothing at runtime, and removing them touches the release DDL, the posture enum, the console, and specification 13 §3.3 at once, which is a larger change than withdrawing four catalog entries | decide whether Solvan ever holds a long-lived customer credential again. If not, retire `STORED_LONG_LIVED`, `credential_scope_verification.py`, and their constraints together, which also closes TD-011 by deletion rather than by adding a second Datadog credential |
| TD-015 | P2 maintainability | five governed code-delivery modules exceed the repository's 700-line Python ceiling while the first deployed CCR qualification is pending | keeping transaction-coupled authority in one store/controller boundary avoids an unsafe cosmetic split before restart, ambiguity, IAM, and rollback tests exercise the deployed path; each exception is owned and expires 2026-09-05, renewed once from 2026-08-20 without the named deployed CCR cases having run -- the renewal is the debt, not a reset of it | after the named deployed CCR cases pass, split qualification receipts, decision challenges, rollout responsibilities, controller handlers, and coordinator schedulers at the exact boundaries recorded in `config/architecture-exceptions.yaml` |
| TD-011 | P3 correctness | Datadog scope verification cannot complete: introspecting an application key also requires the account API key, and a connection carries one `credential_secret_ref`, so a Datadog registration refuses with `INTROSPECTION_REFUSED` | the verification port and its Datadog classification are implemented and the refusal is correct rather than a false pass; only the second credential is missing, and no Datadog connection can reach READY today regardless, since the vendor providers are excluded from capability probing | carry a second scoped secret reference for providers whose introspection needs one, then prove a Datadog key end to end |
| TD-016 | P2 correctness | the actuator alert filters match a log body the runtime demonstrably writes, but the Cloud Logging *projection* of that record — that its `logName` resolves to `solvan-control-plane` and that Python `WARNING` maps to severity `WARNING` — is asserted from the OTel Cloud Logging exporter's documented behaviour rather than observed in a deployed environment | the alternative was to keep no alert at all on the only controls that can halt production mutation; the binding that is checkable locally is checked, in `tests/unit/test_actuator_alert_binding.py`, which composes the real emission with the deployed filter strings rather than asserting either alone | a staging probe engages the kill switch against the deployed actuator and observes `solvan-staging/actuator/kill_switch_engaged` increment within one alignment period; until then do not describe actuator alerting as verified |
| TD-010 | P3 correctness | `guidance_ingestion_receipts` has no `decision_request_id` column or per-request uniqueness, and `guidance_selection_analytics` has no per-delivery key | replay recovery selects the earliest matching receipt after the audit ledger has already proved the material identical, and delivery idempotency is enforced through the `guidance-analytics:` audit namespace, so both are correct but the binding is invisible in the schema | add `decision_request_id` to the ingestion receipt with a scoped unique constraint mirroring `guidance_approvals`, and give selection analytics a first-class delivery key, in the next operability schema expansion |

Resolved items move to the completing execution plan with evidence; do not
delete their history silently.

## Resolved

| ID | Resolved | Evidence |
|---|---|---|
| TD-007 | 2026-08-23 | the exception it recorded no longer exists: `config/architecture-exceptions.yaml` carries no GitHubStore entry, and the ceiling the debt cited as 600 lines is now 700 (`config/architecture-rules.yaml:66`), so the boundary was split without the ledger being updated. Verified by `tools/check_architecture.py` passing with the current exception list |
| TD-012 | 2026-08-17 | the silent-rotation path was the reference, not the cadence: `credential_secret_ref` accepted `versions/latest`, which follows whatever payload was added most recently, so a write-capable key could replace a verified one with nothing in Solvan changing. Both the API pattern and the release DDL now require a pinned numeric version, and a version's payload is immutable while a connection revision is written once and never updated — so the key whose scope was proved is the key every later read resolves, and rotation is a new revision that verifies its own key. A `versions/latest` reference is refused by a must-fail case in `schema-contract-tests.sql` |
| TD-011 (P1 part) | 2026-08-17 | `read_only_scope_verified` is now `GENERATED ALWAYS` over a `read_only_scope_state` enum carrying a closed reason code, verified-at, and evidence reference, so an unverifiable key is recorded non-READY with a legible reason instead of being refused outright, and no writer can set the boolean. Verified against a clean PostgreSQL 16: the schema and its contract tests load, `is_generated` reports `ALWAYS`, and three new must-fail cases prove a direct write raises `generated_always`, an unverified stored key cannot become READY, and an alias reference is refused. Schema revision bumped 68 → 69 so the prior volume is preserved. The residual Datadog second-credential gap continues as TD-011 |
| TD-008 | 2026-08-15 | both flakes had determinate causes, not timing luck: the skill-import fixture built its ZIP without pinning entry timestamps, so two calls straddling ZIP's two-second boundary produced different bytes, a different bundle hash and a different attempt id; and the browser suite ran against a rehearsal database that accumulated turns across runs, so the same suggested question eventually parked instead of answering. The fixture now pins the timestamp (proved byte-identical across a 2.2s gap) and `scripts/check-browser` truncates the conversation ledger first (196 accumulated turns to 16; two consecutive suites green) |
| TD-006 | 2026-08-08 | runtime and deployment use only `WorkspaceAgent`; `google-antigravity==0.1.13` is hash-locked and import-tested; the private regional SDK service, ADK provider, eligibility/attestation, request/result fencing, typed cognition, baseline reproduction, checkpoint/rehydration, retry-safe qualification, Terraform, IAM/egress preflight, console, and negative tests are implemented; historical review/source documents are not executable compatibility surfaces |
| TD-003 | 2026-08-08 | every PR-001–PR-047 named test ID has a checked implementation node in `test-implementations.yaml`; documentation validation rejects missing, extra, or dangling nodes; PostgreSQL and Playwright runners execute their required suites in `scripts/check` |
| TD-005 | 2026-08-08 | all seven size exceptions were removed; stores now separate invocation/material, plan projection, wakeup claims, mitigation policy/rollback, and Runtime queue/reservation responsibilities; coordinator handlers and console projections are decomposed; architecture checks pass with an empty exception list |
| TD-002 | 2026-08-08 | every Python Cloud Run workload now emits W3C-propagated, content-free OTel HTTP spans to Cloud Trace and structured OTel logs to Cloud Logging; Agent Runtime enables native ADK telemetry with message capture disabled; Terraform grants only trace/log/metric writer roles; live correlation remains a release-preflight proof rather than a local claim |
