#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$ROOT"

echo "scripts/cleanup-demo is retained for compatibility; use scripts/restore-fault-drill." >&2
exec scripts/restore-fault-drill "$@"
