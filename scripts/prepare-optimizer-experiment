#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
source "${ROOT_DIR}/scripts/dev-env"

exec uv run python "${ROOT_DIR}/tools/optimize_agent_instructions.py" \
  --output "${SOLVAN_ARTIFACT_DIR}/evals/optimizer-evidence-agent-v1.json"
