# Lookup service topology references

The topology snapshot is produced by registered read-only deployment and
Production Graph tools and frozen into the incident record.

This first-party pack contains no executable files or external credentials.
The coordinator supplies the exact scope and registered profile before any
bounded read.
