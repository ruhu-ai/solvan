# Lookup quota saturation references

Quota and capacity figures come from registered read-only GCP capacity
readers and configured service records.

This first-party pack contains no executable files or external credentials.
The coordinator supplies the exact scope and registered profile before any
bounded read.
