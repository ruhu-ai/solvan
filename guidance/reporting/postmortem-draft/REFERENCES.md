# Postmortem draft references

Sources are the case, transition, action, receipt, patch-review, and
verification records; the draft is stored as an ordinary case artifact.

This first-party pack contains no executable files or external credentials.
The coordinator supplies the exact scope and registered profile before any
bounded read.
