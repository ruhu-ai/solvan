# Triage deployment regression references

Deployment state, traffic split, and change history come from registered
read-only infrastructure tools; this pack proposes no rollback itself.

This first-party pack contains no executable files or external credentials.
The coordinator supplies the exact scope and registered profile before any
bounded read.
