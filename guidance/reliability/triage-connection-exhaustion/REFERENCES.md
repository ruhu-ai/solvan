# Triage connection exhaustion references

Pool metrics, wait-time semantics, and the approved log signature are
supplied by the registered evidence profile at dispatch; this pack names no
query text and holds no credentials.

This first-party pack contains no executable files or external credentials.
The coordinator supplies the exact scope and registered profile before any
bounded read.
