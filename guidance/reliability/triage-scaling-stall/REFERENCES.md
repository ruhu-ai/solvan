# Triage scaling stall references

Scaling metrics and denial signatures come from the registered evidence
profile; configuration values are cited from read-only deployment state.

This first-party pack contains no executable files or external credentials.
The coordinator supplies the exact scope and registered profile before any
bounded read.
